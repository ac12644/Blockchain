
# Consensus mechanism:
# Valid values: pow (Proof-of-Work), pos (Proof-of-Stake)
CONSENSUS_MODE=pos

# Cryptography algorithm:
# Valid values: secp256k1 (classic ECDSA), falcon (post-quantum Falcon-512)
CRYPTO_MODE=falcon

# Node listening port (optional, overrides random selection)
#PORT=8000

# Log level (optional: info, debug, error)
#LOG_LEVEL=info
